---
title: "Second placeholder lesson"
description: "A second placeholder so cross-links and the next-steps section have somewhere to point. Replace or delete this file before launch."
category: "camera-basics"
difficulty: "beginner"
publishedAt: "2026-08-10"
tags: ["placeholder"]
related: ["template-example"]
---

**Placeholder text, not photography instruction.**

This file exists for two reasons: to give the first example lesson a valid target
for its `related` list, and to put a second card in the lessons index so the
listing can be reviewed with more than one row in it.

## Cross-linking

The `related` field takes lesson slugs. The loader checks every one of them at
build time — an unknown slug, or a lesson listing itself, stops the build rather
than rendering a dead link.

## Categories

This lesson sits in a different category from the first, so the category index
pages and the breadcrumb trail can both be checked with real data.
